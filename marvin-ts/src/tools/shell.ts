import type { ToolRegistry } from './registry.js';

export function registerShellTools(registry: ToolRegistry): void {
  // run_command (with confirmation in interactive, auto-approve in non-interactive)
}
