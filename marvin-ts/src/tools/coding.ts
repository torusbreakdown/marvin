import type { ToolRegistry } from './registry.js';

export function registerCodingTools(registry: ToolRegistry): void {
  // set_working_dir, get_working_dir, code_grep, tree, review_codebase, review_status
}
