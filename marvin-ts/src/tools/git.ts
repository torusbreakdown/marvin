import type { ToolRegistry } from './registry.js';

export function registerGitTools(registry: ToolRegistry): void {
  // git_status, git_diff, git_commit, git_log, git_blame, git_branch, git_checkout
}
