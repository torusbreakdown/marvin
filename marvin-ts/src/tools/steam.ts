import type { ToolRegistry } from './registry.js';

export function registerSteamTools(registry: ToolRegistry): void {
  // steam_search, steam_app_details, steam_featured, steam_player_stats, steam_user_*
}
