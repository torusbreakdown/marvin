import type { ToolRegistry } from './registry.js';

export function registerCalendarTools(registry: ToolRegistry): void {
  // calendar_add_event, calendar_delete_event, calendar_view, calendar_list_upcoming
}
